<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!-- jdbc config file name -->
    <properties resource="jdbc.properties" />

    <!-- underscore filed name convet to camelCase convention -->
    <settings>
        <setting name="cacheEnabled" value="true"/>
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>

    <!-- yktdbv2t -->
    <environments default="DDZF.db">
        <!-- 配置开发环境 -->
        <environment id="DDZF.db">
            <!-- jdbc事务管理器 -->
            <transactionManager type="JDBC"/>
            <!-- 数据源
             UNPOOLED 不适用连接池 即 每次请求都会为其创建一个DB连接，适用完毕后，会马上将连接关闭
             POOLED 数据库连接池来维护连接
             JNDI 数据源可以定义到应用的外部，通过JDNI容器来获取数据库连接
             -->
            <dataSource type="POOLED">
                <property name="driver" value="${ddzf.jdbc.driverClass}"/>
                <property name="url" value="${ddzf.jdbc.url}"/>
                <property name="username" value="${ddzf.jdbc.username}"/>
                <property name="password" value="${ddzf.jdbc.password}"/>
            </dataSource>
        </environment>

    </environments>

    <!-- mappers -->
    <mappers>
        <!-- 注册映射文件 -->
        <mapper resource="mappers/TradeDataMapper.xml"></mapper>
        <mapper resource="mappers/TradeDeviceMapper.xml"></mapper>
    </mappers>
</configuration>