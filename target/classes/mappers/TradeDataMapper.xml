<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.TradeDataDao">

    <select id="listDatasByDeviceId" resultType="pojo.TradeDataDO" parameterType="Map">
        SELECT *
        FROM t_trade_data where pos_id = #{deviceNo} and create_time > #{startTime} order by create_time asc limit #{limit}
    </select>
</mapper>